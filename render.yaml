services:
  # Frontend Service (Unified - serves React build via Express)
  - type: web
    name: bian-gtc
    env: node
    region: oregon
    plan: free
    buildCommand: |
      echo "🎨 Building Frontend for bian-gtc service..."
      echo "📁 Current directory: $(pwd)"
      
      echo "📦 Installing backend dependencies..."
      cd backend && npm install --production
      
      echo "📦 Installing frontend dependencies..."
      cd ../frontend && npm install --production
      
      echo "🔨 Building frontend for production..."
      npm run build
      
      echo "✅ Verifying build output..."
      if [ -d "dist" ]; then
        echo "✅ Frontend build successful - dist/ directory created"
      else
        echo "❌ Frontend build failed - dist/ directory not found"
        exit 1
      fi
      
      echo "🚀 Frontend build completed successfully!"
    startCommand: cd backend && npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DEBUG
        value: "ON"
      - key: PORT
        value: 10000
      - key: VITE_API_URL
        value: https://bian-api-backend.onrender.com/api
      - key: VITE_GOOGLE_CLIENT_ID
        fromSecret: true

  # Backend API Service  
  - type: web
    name: bian-api-backend
    env: node
    region: oregon
    plan: free
    buildCommand: |
      echo "⚙️ Building Backend Service..."
      cd backend
      npm install --production
      echo "✅ Backend build completed"
    startCommand: cd backend && npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DEBUG
        value: "ON"
      - key: MONGODB_URI
        fromSecret: true
      - key: GOOGLE_CLIENT_ID
        fromSecret: true
      - key: GOOGLE_CLIENT_SECRET
        fromSecret: true
      - key: OPENAI_API_KEY
        fromSecret: true
      - key: JWT_SECRET
        fromSecret: true
      - key: PORT
        value: 10000
      - key: FRONTEND_URL
        value: https://bian-gtc.onrender.com 